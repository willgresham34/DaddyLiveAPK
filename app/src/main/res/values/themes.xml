<resources>
    <style name="Theme.DaddyLive" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Customize colors/overrides if needed -->
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
